# Postgres connection string
DATABASE_URL=postgres://user:password@localhost:5432/escribe

# Optional: CA certificate for SSL (path to PEM file)
DB_CA_CERT_PATH=./db/ca.pem

# API server port
PORT=4000

# CORS origin for the frontend
CORS_ORIGIN=http://localhost:5173

# Frontend API base (Vite)
VITE_API_BASE=http://localhost:4000
